# This file contains the necessary modules to run the code
# Install by running the following command in the terminal in the same directory as this file
# python -m pip install -r requirements.txt

# Necessary modules
openai
flask
gunicorn
flask-cors
wsgi
